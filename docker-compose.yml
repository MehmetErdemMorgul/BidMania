services:
  mongodb:
    image: mongo:latest
    container_name: bidmania_mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=AuctionDb
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: